---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: canaddresspools.z21.trains.io
spec:
  group: z21.trains.io
  names:
    kind: CANAddressPool
    listKind: CANAddressPoolList
    plural: canaddresspools
    shortNames:
    - canpool
    singular: canaddresspool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.start
      name: Range
      type: string
    - jsonPath: .spec.end
      name: End
      type: string
    - jsonPath: .status.allocated
      name: Allocated
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CANAddressPool is a module address range shared by detectors
          on one layout.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CANAddressPoolSpec defines an allocatable module address
              range.
            properties:
              end:
                description: End is the last auto-allocatable module address (inclusive).
                maximum: 255
                minimum: 1
                type: integer
              reserved:
                description: Reserved addresses are never auto-allocated; they may
                  still be claimed explicitly.
                items:
                  type: integer
                type: array
              start:
                description: Start is the first auto-allocatable module address (inclusive).
                maximum: 255
                minimum: 1
                type: integer
            required:
            - end
            - start
            type: object
          status:
            description: CANAddressPoolStatus summarizes pool usage (aggregated by
              the CANDetector controller).
            properties:
              allocated:
                description: Allocated is the number of addresses currently assigned
                  to detectors.
                type: integer
              autoAvailable:
                description: AutoAvailable is the number of addresses Allocate may
                  still choose.
                type: integer
              rangeSize:
                description: RangeSize is the number of addresses between start and
                  end inclusive.
                type: integer
              reserved:
                description: Reserved is the number of reserved addresses in spec.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
